# ------------------------------------------------------------------------------
# Самое важное для Docker (чего не было в твоем списке)
# ------------------------------------------------------------------------------
.git
.gitignore
Dockerfile
docker-compose*
README.md
LICENSE

# Husky (хуки git не нужны внутри продакшн контейнера)
.husky

# ------------------------------------------------------------------------------
# Твой список (адаптированный)
# ------------------------------------------------------------------------------

# dependencies
# ВАЖНО: Мы игнорируем локальные node_modules, чтобы Docker установил 
# свои (под Linux Alpine), иначе будут ошибки несовместимости систем.
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
# Игнорируем локальные билды, Docker соберет всё сам
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
# КРИТИЧНО: Никогда не копируй локальные .env файлы в образ!
# Переменные должны передаваться через docker-compose
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# clerk & editor configs
/.clerk/
.cursor
.cursorrules
.vscode
.idea